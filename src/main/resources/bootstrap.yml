spring:
  application.name: app-authorization-server
  cloud:
    config:
      uri: http://utils-config-server:8888
      fail-fast: true
      username: user
      password: none
  main:
    allow-bean-definition-overriding: true
